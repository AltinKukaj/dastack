/**
 * Shared application constants.
 *
 * Single source of truth for brand name and other global config
 * that template users should customize once.
 */

/** Display name used in emails, auth prompts, and meta tags. */
export const APP_NAME = "DaStack";
